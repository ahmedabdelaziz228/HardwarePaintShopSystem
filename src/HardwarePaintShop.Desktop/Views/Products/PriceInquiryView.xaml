<UserControl x:Class="HardwarePaintShop.Desktop.Views.Products.PriceInquiryView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d"
             FlowDirection="RightToLeft"
             Background="{StaticResource ContentBackgroundBrush}">
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <Border Grid.Row="0" Style="{StaticResource CardStyle}" Margin="0,0,0,12">
            <StackPanel>
                <TextBlock Text="استعلام سريع عن السعر" FontSize="21" FontWeight="Bold"
                           Foreground="{StaticResource TextPrimaryBrush}" Margin="0,0,0,4"/>
                <TextBlock Text="سعر البيع، أقل سعر، الرصيد بالوحدات، المورد الرئيسي وآخر تكلفة للمصرّح لهم."
                           Foreground="{StaticResource MutedBrush}" Margin="0,0,0,14"/>
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="220"/>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>
                    <TextBox x:Name="InquirySearchBox" Grid.Column="0"
                             Text="{Binding SearchText, UpdateSourceTrigger=PropertyChanged}"
                             Style="{StaticResource AppTextBoxStyle}" FontSize="17" Height="46"
                             KeyDown="InquirySearchBox_KeyDown" Margin="0,0,10,0"
                             ToolTip="اسم / كود / باركود / سيريال"/>
                    <ComboBox Grid.Column="1" ItemsSource="{Binding PriceGroupOptions}"
                              SelectedValue="{Binding SelectedPriceGroupId}"
                              SelectedValuePath="Id" DisplayMemberPath="Name"
                              Style="{StaticResource AppComboBoxStyle}" Height="46" Margin="0,0,10,0"/>
                    <Button Grid.Column="2" Content="🔍 استعلام"
                            Command="{Binding SearchCommand}" Style="{StaticResource PrimaryButtonStyle}"
                            Padding="24,10" Margin="0,0,8,0"/>
                    <Button Grid.Column="3" Content="مسح"
                            Command="{Binding ClearCommand}" Style="{StaticResource SecondaryButtonStyle}"/>
                </Grid>
                <ProgressBar IsIndeterminate="True" Height="3"
                             Visibility="{Binding IsBusy, Converter={StaticResource BoolToVisibilityConverter}}"
                             Foreground="{StaticResource PrimaryBrush}" Margin="0,10,0,0"/>
            </StackPanel>
        </Border>

        <Border Grid.Row="1" Padding="12,8" CornerRadius="6" Margin="0,0,0,12">
            <Border.Style>
                <Style TargetType="Border">
                    <Setter Property="Background" Value="#FFEBEE"/>
                    <Style.Triggers>
                        <DataTrigger Binding="{Binding IsSuccess}" Value="True">
                            <Setter Property="Background" Value="#E8F5E9"/>
                        </DataTrigger>
                    </Style.Triggers>
                </Style>
            </Border.Style>
            <TextBlock Text="{Binding StatusMessage}" FontSize="14">
                <TextBlock.Style>
                    <Style TargetType="TextBlock">
                        <Setter Property="Foreground" Value="{StaticResource ErrorBrush}"/>
                        <Style.Triggers>
                            <DataTrigger Binding="{Binding IsSuccess}" Value="True">
                                <Setter Property="Foreground" Value="{StaticResource SuccessBrush}"/>
                            </DataTrigger>
                        </Style.Triggers>
                    </Style>
                </TextBlock.Style>
            </TextBlock>
        </Border>

        <Border Grid.Row="2" Style="{StaticResource CardStyle}">
            <DataGrid ItemsSource="{Binding Results}" Style="{StaticResource AppDataGridStyle}"
                      FontSize="14" RowHeight="48">
                <DataGrid.Columns>
                    <DataGridTextColumn Header="الكود" Binding="{Binding ProductCode}" Width="100"/>
                    <DataGridTextColumn Header="المنتج" Binding="{Binding ProductName}" Width="*"/>
                    <DataGridTextColumn Header="التصنيف" Binding="{Binding CategoryName}" Width="110"/>
                    <DataGridTextColumn Header="المورد" Binding="{Binding MainSupplierName}" Width="120"/>
                    <DataGridTextColumn Header="الوحدة" Binding="{Binding UnitName}" Width="100"/>
                    <DataGridTextColumn Header="فئة السعر" Binding="{Binding PriceGroupName}" Width="130"/>
                    <DataGridTextColumn Header="سعر البيع" Binding="{Binding SalePrice, StringFormat=N2}" Width="115">
                        <DataGridTextColumn.ElementStyle>
                            <Style TargetType="TextBlock">
                                <Setter Property="FontSize" Value="17"/>
                                <Setter Property="FontWeight" Value="Bold"/>
                                <Setter Property="Foreground" Value="{StaticResource PrimaryBrush}"/>
                                <Setter Property="VerticalAlignment" Value="Center"/>
                            </Style>
                        </DataGridTextColumn.ElementStyle>
                    </DataGridTextColumn>
                    <DataGridTextColumn Header="أقل سعر" Binding="{Binding MinSalePrice, StringFormat=N2}" Width="95"/>
                    <DataGridTextColumn Header="الرصيد" Binding="{Binding StockDisplay}" Width="140"/>
                    <DataGridTextColumn Header="آخر تكلفة أساسي" Binding="{Binding LastPurchasePriceBaseUnit, StringFormat=N2}" Width="115"/>
                    <DataGridTextColumn Header="الباركود" Binding="{Binding Barcode}" Width="130"/>
                    <DataGridTextColumn Header="نوع المطابقة" Binding="{Binding MatchSource}" Width="100"/>
                </DataGrid.Columns>
            </DataGrid>
        </Border>
    </Grid>
</UserControl>
